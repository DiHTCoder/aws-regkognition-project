var AWS = require("aws-sdk");
const fs = require("fs");
require("dotenv").config();

AWS.config.update({
  accessKeyId: "",
  accessKeyId: "",
  secretAccessKey: "",
  sessionToken: "",
  region: "",
});
